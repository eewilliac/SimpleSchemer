#lang racket

(define simple1 (list (list (list 'hotdogs)) (list 'and) (list 'pickle) 'relish))
(define simple2 (list (list (list 'hotdogs)) (list 'and)))

(define alist '())
(define simpleList (list 'elem1 'elem2))
(define alist2 '(x y)) ;this is the new way I should make lists.

(define simple '(((hotdog))(and)(pickle))) ;this is the new way I should make lists.
